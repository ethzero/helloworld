version: '3.8'
services:
  hello-world-nodejs:
    build:
      context: ./hello-world-nodejs
    ports:
      - 8080:8080
    # volumes: 
    #   - ./:/hello-world-volume
    # command: npm run debug

#   mongo:
#     image: mongo:4.2.8
#     ports:
#       - 27017:27017
#     volumes: 
#       - mongodb:/data/db
#       - mongodb_config:/data/configdb

# volumes: 
#   mongodb:
#   mongodb_config:
      
